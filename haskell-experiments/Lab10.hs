module Main where

import Prelude (Ord, (<=), (>=), (-), Bool, IO, putStrLn, (<$>), pure, (<*>), undefined)
import Bool
import Functions
import List
import Maybe

maximum :: Ord a => [a] -> Maybe a
maximum [] = Nothing
maximum a = undefined 

main :: IO()
main = undefined
